<% if current_user %>
<a onclick="return false;" href="javascript:var d=document,w=window,e=w.getSelection,x=d.selection,s='';if(x){var r=x.createRange();if(r.htmlText){s=r.htmlText;}else if(r.length>=1){s=r.item(0).outerHTML;}}else{r=e();if(r.rangeCount>0 && w.XMLSerializer){r=r.getRangeAt(0);s=new XMLSerializer().serializeToString(r.cloneContents());}};var f=d.createElement('form');f.setAttribute('method','post');f.setAttribute('action','http://<%= Settings.hostname -%>/quotes.js?url='+encodeURIComponent(location.href)+'&title='+encodeURIComponent(d.title)+'&token='+encodeURIComponent('<%= current_user.submit_token -%>'));var h=d.createElement('input');h.setAttribute('type','hidden');h.setAttribute('name','body');h.setAttribute('value',s);f.appendChild(h);d.body.appendChild(f);f.submit();void 1;" title="Drag this 'bookmark' to the bookmarks in your browser">bookmark</a>
<% end %>
